import styled from 'styled-components'

import { accent } from '../../resources/color'

const H2 = styled.h2`
  font-size: 20px;

  color: ${accent};
  text-align: center;
  font-weight: 100;
`

export default H2
